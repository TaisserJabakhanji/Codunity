export type Course = {
  title: string;
  description: string;
  level: string;
  image: string;
};

export type Instructor = {
  name: string,
  title: string,
  specialty: string,
  bio: string,
  avatar: string
}